namespace Domain.Entities;

public class RealPerson : Person
{
    public RealPerson(string name, string birthDate, string nationality, string photo) : base(name, birthDate, nationality, photo)
    {
    }
}

